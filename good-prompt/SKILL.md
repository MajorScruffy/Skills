---
name: good-prompt
description: Write or edit a skill, prompt (including in code), or context file (AGENTS.md).
---

# Good prompt

Write only what the agent must use to succeed, and end with what the agent must do. No redundancy. Name the shortest output that still completes the task — each output token adds latency, so faster answers need fewer of them. A skill description is loaded on every turn, so keep it short.
